

# Yay




## Subject

* [Link](#link)
* [AUR Package](#aur-package)
* [Installation](#installation)




## Link

* https://github.com/Jguer/yay
* https://github.com/Jguer/yay/blob/next/README.md
* https://raw.githubusercontent.com/Jguer/yay/refs/heads/next/README.md




## AUR Package

> Search: [yay](https://aur.archlinux.org/packages?O=0&SeB=nd&K=yay&outdated=&SB=p&SO=d&PP=50&submit=Go)

| AUR Package |
| ----------- |
| [yay](https://aur.archlinux.org/packages/yay) |
| [yay-bin](https://aur.archlinux.org/packages/yay-bin) |




## Installation




### Source

The initial installation of Yay can be done by cloning the PKGBUILD and
building with makepkg:

We make sure we have the `base-devel` package group installed.

```sh
sudo pacman -S --needed git base-devel
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
```

If you want to do all of this at once, we can chain the commands like so:

```sh
sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si
```




### Binary

If you do not want to compile yay yourself you can use the builds generated by
GitHub Actions.

```sh
sudo pacman -S --needed git base-devel
git clone https://aur.archlinux.org/yay-bin.git
cd yay-bin
makepkg -si
```

If you want to do all of this at once, we can chain the commands like so:

```sh
sudo pacman -S --needed git base-devel && git clone https://aur.archlinux.org/yay-bin.git && cd yay-bin && makepkg -si
```
